// Theme presets for CelestialUI
import type { ThemeConfig } from './index'

export const lightPreset: Partial<ThemeConfig> = {
  name: 'light',
  displayName: 'Light Theme',
  type: 'light'
}

export const darkPreset: Partial<ThemeConfig> = {
  name: 'dark',
  displayName: 'Dark Theme',
  type: 'dark'
}

export const highContrastPreset: Partial<ThemeConfig> = {
  name: 'high-contrast',
  displayName: 'High Contrast',
  type: 'light'
}








